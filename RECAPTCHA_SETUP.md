# Настройка Google reCAPTCHA v3 - Подробная инструкция

## 📋 Шаг 1: Откройте консоль Google reCAPTCHA

1. Перейдите по адресу: **https://www.google.com/recaptcha/admin**
2. Войдите в свой аккаунт Google (если еще не вошли)

## 📋 Шаг 2: Создайте новый сайт

1. В правом верхнем углу нажмите кнопку **"+"** (плюс) или **"Create"** / **"Register a new site"**
2. Откроется форма регистрации

## 📋 Шаг 3: Заполните форму

### Поле "Label" (Метка)
- Введите: `Web-Alchemy` или любое другое имя для идентификации
- Это имя нужно только для вас, чтобы отличать ключи

### Поле "reCAPTCHA type" (Тип reCAPTCHA)
- **Выберите:** `reCAPTCHA v3` ⚠️ ВАЖНО: именно v3, не v2!

### Поле "Domains" (Домены)
Добавьте домены, на которых будет работать reCAPTCHA. Каждый домен с новой строки:

**Для локальной разработки:**
```
localhost
127.0.0.1
```

**Для Vercel (если используете):**
```
localhost
web-alchemy.vercel.app
your-custom-domain.vercel.app
```

**Для production домена (если есть):**
```
your-domain.com
www.your-domain.com
```

⚠️ **Важно:**
- Не добавляйте `http://` или `https://`
- Не добавляйте порты (например, `localhost:3000` - неправильно!)
- Только доменное имя: `localhost`, `example.com`
- Можно добавить поддомены: `www.example.com`, `api.example.com`

### Чекбоксы
- ✅ Отметьте согласие с условиями использования (Terms of Service)
- ✅ Отметьте, если хотите получать email-уведомления (опционально)

## 📋 Шаг 4: Отправьте форму

1. Нажмите кнопку **"Submit"** (Отправить)
2. Google создаст ключи

## 📋 Шаг 5: Скопируйте ключи

После создания вы увидите два ключа:

### Site Key (Публичный ключ)
- Начинается с `6L...`
- Пример: `6LeIxAcTAAAAAJcZVRqyHh71UMIEGNQ_MXjiZKhI`
- Этот ключ будет виден в коде сайта (это нормально)

### Secret Key (Секретный ключ)
- Начинается с `6L...` (но другой)
- Пример: `6LeIxAcTAAAAAGG-vFI1TnRWxMZNFuojJ4WifJWe`
- ⚠️ **НИКОГДА не публикуйте этот ключ!** Он должен быть только в `.env.local` и на сервере

## 📋 Шаг 6: Вставьте ключи в `.env.local`

Откройте файл `.env.local` в корне проекта и заполните:

```bash
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=вставьте_сюда_site_key
RECAPTCHA_SECRET_KEY=вставьте_сюда_secret_key
```

## 📋 Шаг 7: Перезапустите сервер

```bash
npm run dev
```

## ✅ Проверка

После перезапуска:
1. Откройте форму контактов
2. Откройте консоль браузера (F12 → Console)
3. При отправке формы должны увидеть сообщения о reCAPTCHA

---

## 🔒 Настройки безопасности (опционально)

В консоли reCAPTCHA можно:
- Ограничить домены, с которых можно использовать ключи
- Посмотреть статистику использования
- Настроить уведомления

## ❓ Частые проблемы

### reCAPTCHA не работает на localhost
- Убедитесь, что добавили `localhost` в список доменов в Google Console
- Перезапустите dev-сервер после добавления переменных окружения

### Ошибка "Invalid site key"
- Проверьте, что скопировали Site Key полностью (без пробелов)
- Убедитесь, что используете ключ от reCAPTCHA v3, а не v2

### reCAPTCHA не работает на Vercel
- Добавьте переменные окружения в Vercel Dashboard
- Убедитесь, что добавили домен Vercel (`*.vercel.app`) в Google Console

---

**Нужна помощь?** Если возникли вопросы на каком-то шаге, опишите на каком именно шаге застряли!
