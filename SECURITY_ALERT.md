# ⚠️ ВАЖНО: Безопасность - раскрытие ключей reCAPTCHA

## 🚨 Обнаружена проблема безопасности

GitGuardian обнаружил, что реальные ключи reCAPTCHA были закоммичены в репозиторий GitHub в файлах документации.

### 📋 Что было обнаружено:

- **NEXT_PUBLIC_RECAPTCHA_SITE_KEY**: `6Lcdl04sAAAAAKvUfYaHE55nbqc6Oq2rqzaix4e2`
- **RECAPTCHA_SECRET_KEY**: `6Lcdl04sAAAAAHSHsEh6HtAwwYDTj7Zj98cAfDZs`

### ✅ Что было исправлено:

- Реальные ключи заменены на placeholder'ы (`your_site_key_here`, `your_secret_key_here`) во всех файлах документации
- Файлы проверены и исправлены:
  - `RESEND_SETUP.md`
  - `FIX_RECAPTCHA_CACHE.md`
  - `RECAPTCHA_CHECKLIST.md`

### 🔐 Что нужно сделать СРОЧНО:

#### 1. **СБРОСИТЬ РАСКРЫТЫЕ КЛЮЧИ reCAPTCHA**

1. Перейдите на https://www.google.com/recaptcha/admin
2. Войдите в аккаунт Google
3. Найдите ваш проект reCAPTCHA
4. **Удалите старый проект** или **сбросьте ключи** (если возможно)

#### 2. **СОЗДАТЬ НОВЫЕ КЛЮЧИ**

1. Создайте новый проект reCAPTCHA:
   - Label: `Web-Alchemy Production` (или новое имя)
   - reCAPTCHA type: `reCAPTCHA v3`
   - Domains: добавьте все ваши домены (localhost, vercel.app, production domain)

2. Скопируйте новые ключи:
   - **Site Key** (начинается с `6L...`)
   - **Secret Key** (начинается с `6L...`)

#### 3. **ОБНОВИТЬ ПЕРЕМЕННЫЕ ОКРУЖЕНИЯ**

**Локально (`.env.local`):**
```bash
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=новый_site_key
RECAPTCHA_SECRET_KEY=новый_secret_key
```

**На Vercel:**
1. Vercel Dashboard → Ваш проект → Settings → Environment Variables
2. Обновите:
   - `NEXT_PUBLIC_RECAPTCHA_SITE_KEY`
   - `RECAPTCHA_SECRET_KEY`
3. Используйте новые ключи
4. Перезапустите deployment

### 🔄 Удаление старых ключей из истории Git (опционально, но рекомендуется)

Если хотите полностью удалить раскрытые ключи из истории Git:

```bash
# ВНИМАНИЕ: Это перепишет историю Git!
# Используйте только если уверены и сделали backup

git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch FIX_RECAPTCHA_CACHE.md RECAPTCHA_CHECKLIST.md RESEND_SETUP.md" \
  --prune-empty --tag-name-filter cat -- --all

git push --force --all
```

**⚠️ ПРЕДУПРЕЖДЕНИЕ:** Это перепишет историю Git. Все, кто клонировал репозиторий, должны будут переклонировать его.

### 📝 Рекомендации на будущее:

1. **НИКОГДА не коммитьте реальные ключи** в репозиторий
2. Используйте `.env.example` с placeholder'ами
3. Всегда проверяйте файлы перед коммитом: `git diff`
4. Используйте инструменты как GitGuardian для автоматического обнаружения секретов

### 📚 Полезные ссылки:

- Google reCAPTCHA Admin: https://www.google.com/recaptcha/admin
- GitGuardian: https://www.gitguardian.com

---

**Дата обнаружения:** 19 января 2026  
**Статус:** ✅ Ключи удалены из файлов документации  
**Требуется действие:** 🔴 Сбросить и создать новые ключи в Google Console
