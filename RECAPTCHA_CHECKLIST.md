# ✅ Чеклист: Проверка reCAPTCHA

## 🔍 Шаг 1: Проверьте настройки домена в Google Console

На странице, которую вы сейчас видите:

1. **Найдите кнопку "Settings" (Настройки)** или "Edit" (Редактировать)
   - Обычно в правом верхнем углу или в навигации

2. **Откройте настройки сайта**

3. **Проверьте поле "Domains" (Домены)**

   **Там должно быть:**
   ```
   localhost
   ```

   ⚠️ **Важно:**
   - БЕЗ `http://` или `https://`
   - БЕЗ портов (не `localhost:3000`)
   - Только `localhost`

4. **Если `localhost` отсутствует:**
   - Добавьте его в поле "Domains"
   - Нажмите **"Save"** (Сохранить)

---

## 🔄 Шаг 2: Перезапустите dev-сервер

**Next.js загружает переменные окружения только при старте!**

1. Остановите текущий сервер (`Ctrl+C` в терминале)
2. Запустите заново:
   ```bash
   npm run dev
   ```

---

## 🌐 Шаг 3: Очистите кэш браузера

1. Откройте DevTools (F12)
2. Нажмите правой кнопкой на кнопку обновления страницы
3. Выберите **"Очистить кэш и жесткая перезагрузка"**
   - Или: `Cmd+Shift+R` (Mac) / `Ctrl+Shift+R` (Windows)

---

## ✅ Шаг 4: Проверьте работу

1. Откройте форму контактов на сайте
2. Откройте консоль браузера (F12 → Console)
3. Заполните и отправьте форму
4. Проверьте консоль — не должно быть ошибок 401

---

## 📝 Шаг 5: Проверьте ключи в коде

Убедитесь, что ключи правильно используются:

```bash
# В терминале проверьте:
cat .env.local
```

Должно быть:
```
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=6Lcdl04sAAAAAKvUfYaHE55nbqc6Oq2rqzaix4e2
RECAPTCHA_SECRET_KEY=6Lcdl04sAAAAAHSHsEh6HtAwwYDTj7Zj98cAfDZs
```

---

## ❓ Если всё ещё не работает

1. **Проверьте Site Key в Google Console:**
   - Он должен совпадать с тем, что в `.env.local`
   
2. **Проверьте домены еще раз:**
   - Убедитесь, что `localhost` добавлен БЕЗ пробелов

3. **Перезапустите сервер снова:**
   ```bash
   # Полностью остановите сервер (Ctrl+C)
   # Затем запустите заново
   npm run dev
   ```

---

## ✅ Готово!

После этих шагов reCAPTCHA должна работать корректно.

**Статистика в Google Console обновится после первых успешных проверок!**
