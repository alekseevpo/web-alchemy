# 🔧 Исправление ошибки 401 от Google reCAPTCHA

## ❌ Проблема:
`POST https://www.google.com/recaptcha/api2/pat?k=... 401 (Unauthorized)`

## 🔍 Причины:

1. **Домен `localhost` не добавлен в Google Console**
2. **Сервер не был перезапущен после добавления ключей**

---

## ✅ Решение:

### Шаг 1: Проверьте домены в Google Console

1. Откройте: **https://www.google.com/recaptcha/admin**
2. Найдите ваш сайт (например, "Web-Alchemy") и нажмите на него
3. На странице настроек проверьте поле **"Domains"** (Домены)

**Убедитесь, что там есть:**
```
localhost
```

⚠️ **Важно:**
- БЕЗ `http://` или `https://`
- БЕЗ портов (не `localhost:3000`)
- Только `localhost`

**Если `localhost` нет:**
1. Нажмите кнопку **"Edit"** (Редактировать) или **"Settings"** (Настройки)
2. Добавьте `localhost` в поле "Domains"
3. Нажмите **"Save"** (Сохранить)

---

### Шаг 2: Перезапустите dev-сервер

**Next.js загружает переменные окружения только при старте!**

1. Остановите текущий dev-сервер (нажмите `Ctrl+C` в терминале)
2. Запустите заново:
   ```bash
   npm run dev
   ```

---

### Шаг 3: Очистите кэш браузера

1. Откройте DevTools (F12)
2. Нажмите правой кнопкой на кнопку обновления
3. Выберите **"Очистить кэш и жесткая перезагрузка"** (или `Cmd+Shift+R` / `Ctrl+Shift+R`)

---

## ✅ После исправления:

Ошибка 401 должна исчезнуть, и reCAPTCHA будет работать корректно.

**Проверка:**
- В консоли не должно быть ошибок 401
- Предупреждение `'private-token'` можно игнорировать (это не критично)
