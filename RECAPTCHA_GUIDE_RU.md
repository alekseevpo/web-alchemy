# 📝 Пошаговая инструкция: Создание reCAPTCHA v3

## 🎯 Шаг 1: Откройте консоль Google

1. **Откройте браузер** (Chrome, Safari, Firefox и т.д.)
2. Перейдите по адресу: **https://www.google.com/recaptcha/admin**
3. **Войдите в Google-аккаунт**, если еще не вошли

---

## ➕ Шаг 2: Создайте новый сайт

После входа вы увидите страницу с вашими сайтами (если они есть).

1. **Нажмите кнопку "+"** в правом верхнем углу (или кнопку **"Register a new site"** / **"Create"**)
2. Откроется форма регистрации

---

## 📝 Шаг 3: Заполните форму

### 1. Поле "Label" (Метка)
- **Введите:** `Web-Alchemy` (или любое другое имя для вас)
- Это просто название для идентификации в списке

### 2. Поле "reCAPTCHA type" (Тип reCAPTCHA)
- **Выберите:** `reCAPTCHA v3` ⚠️ ВАЖНО: именно v3!

**Не выбирайте:**
- ❌ reCAPTCHA v2 "I'm not a robot" Checkbox
- ❌ reCAPTCHA v2 "I'm not a robot" Invisible

**Выбирайте:**
- ✅ reCAPTCHA v3

### 3. Поле "Domains" (Домены)

В это поле нужно добавить домены, на которых будет работать reCAPTCHA.

**Для локальной разработки:**
```
localhost
```

**Если используете Vercel:**
```
localhost
web-alchemy.vercel.app
```

**Если у вас есть свой домен:**
```
localhost
your-domain.com
www.your-domain.com
```

⚠️ **Важно:**
- Каждый домен на **отдельной строке**
- **БЕЗ** `http://` или `https://`
- **БЕЗ** портов (не `localhost:3000`, просто `localhost`)
- Только доменное имя

### 4. Чекбоксы
- ✅ Отметьте **согласие с условиями использования** (Terms of Service)
- (Опционально) Отметьте, если хотите получать email-уведомления

---

## ✅ Шаг 4: Отправьте форму

1. Нажмите кнопку **"Submit"** (Отправить)
2. Google создаст ключи

---

## 🔑 Шаг 5: Скопируйте ключи

После создания вы увидите страницу с **двумя ключами**:

### Site Key (Публичный ключ)
- Начинается с `6L...`
- Рядом есть кнопка **"Copy"** (Копировать)
- Пример: `6LeIxAcTAAAAAJcZVRqyHh71UMIEGNQ_MXjiZKhI`

### Secret Key (Секретный ключ)
- Находится ниже Site Key
- Начинается с `6L...` (но другой!)
- Рядом есть кнопка **"Copy"** (Копировать)
- Пример: `6LeIxAcTAAAAAGG-vFI1TnRWxMZNFuojJ4WifJWe`

**Скопируйте оба ключа** (можно кнопкой "Copy" или выделить текст и Ctrl+C / Cmd+C)

---

## 📂 Шаг 6: Вставьте ключи в `.env.local`

Откройте файл `.env.local` в корне проекта и заполните:

```bash
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=вставьте_site_key_сюда
RECAPTCHA_SECRET_KEY=вставьте_secret_key_сюда
```

---

## 🔄 Шаг 7: Перезапустите dev-сервер

```bash
npm run dev
```

---

## ❓ Если не видите ключи

1. Проверьте список сайтов на главной странице консоли
2. Нажмите на название вашего сайта (например, "Web-Alchemy")
3. На странице настроек будут оба ключа

---

## ✅ Готово!

После этого reCAPTCHA v3 будет работать на вашем сайте.
