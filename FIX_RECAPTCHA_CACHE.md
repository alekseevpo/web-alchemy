# 🔧 Исправление проблемы с кэшем reCAPTCHA

## ❌ Проблема:
Браузер использует **старый кэшированный** ключ reCAPTCHA
Вместо правильного из `.env.local`

## 🔍 Причина:
Браузер закэшировал скрипт reCAPTCHA с другим ключом.

---

## ✅ Решение (по порядку):

### 1. Полная очистка кэша браузера

**Chrome:**
1. Нажмите `Cmd+Shift+Delete` (Mac) или `Ctrl+Shift+Delete` (Windows)
2. Выберите **"За все время"** (All time)
3. Отметьте:
   - ✅ Изображения и файлы в кэше
   - ✅ Cookies и другие данные сайтов
4. Нажмите **"Очистить данные"**

### 2. Жесткая перезагрузка

**Вариант 1:**
- `Cmd+Shift+R` (Mac) или `Ctrl+Shift+R` (Windows)

**Вариант 2:**
- Откройте DevTools (F12)
- Правой кнопкой на кнопку обновления
- Выберите **"Очистить кэш и жесткая перезагрузка"**

### 3. Проверьте в режиме инкогнито

1. Откройте новое окно инкогнито: `Cmd+Shift+N` (Mac) или `Ctrl+Shift+N` (Windows)
2. Перейдите на `localhost:3000`
3. Проверьте работу reCAPTCHA

### 4. Проверьте загруженный скрипт

1. Откройте DevTools (F12)
2. Перейдите на вкладку **Network** (Сеть)
3. Обновите страницу
4. Найдите запрос к `recaptcha/api.js?render=...`
5. Проверьте, что в URL правильный ключ из `.env.local`

---

## 🔄 Если не помогло:

### Перезапустите сервер заново:

```bash
# Остановите сервер
Ctrl+C

# Убедитесь, что процесс остановлен
lsof -ti:3000 | xargs kill -9

# Запустите заново
npm run dev
```

### Проверьте .env.local еще раз:

```bash
cat .env.local
```

Должно быть:
```
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=your_site_key_here
RECAPTCHA_SECRET_KEY=your_secret_key_here
```

---

## ✅ После очистки:

1. Откройте форму контактов
2. Откройте консоль (F12)
3. Заполните и отправьте форму
4. **Ошибка 401 должна исчезнуть**

---

**Примечание:** Предупреждение `'private-token'` можно игнорировать - это не критично.
